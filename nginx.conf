user  root;
worker_processes  1;

events {
}

http {
    server {
        listen       81;
        server_name  lms.mashinarium45.ru; 

        location / {
            proxy_pass http://frontend:3000/;
        }

        location /api/ {
            proxy_pass http://backend:8000/;
        }
    }
}
